<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mungtrainer.mtserver.dog.dao.DogDAO">

    <!-- Result Map 정의 -->
    <resultMap id="dogResultMap" type="com.mungtrainer.mtserver.dog.dto.response.DogResponse">
        <id property="dogId" column="dog_id"/>
        <result property="name" column="name"/>
        <result property="breed" column="breed"/>
        <result property="age" column="age"/>
        <result property="gender" column="gender"/>
        <result property="isNeutered" column="is_neutered"/>
        <result property="weight" column="weight"/>
        <result property="personality" column="personality"/>
        <result property="habits" column="habits"/>
        <result property="healthInfo" column="health_info"/>
        <result property="profileImage" column="profile_image"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- 반려견 생성 -->
    <insert id="insertDog" parameterType="map">
        INSERT INTO dog (
            user_id,
            name,
            breed,
            age,
            gender,
            is_neutered,
            weight,
            personality,
            habits,
            health_info,
            profile_image,
            created_by,
            created_at,
            updated_by,
            updated_at,
            is_deleted
        ) VALUES (
            #{userId},
            #{request.name},
            #{request.breed},
            #{request.age},
            #{request.gender},
            #{request.isNeutered},
            #{request.weight},
            #{request.personality},
            #{request.habits},
            #{request.healthInfo},
            #{request.profileImageUrl},
            #{createdBy},
            NOW(),
            #{updatedBy},
            NOW(),
            0
        )
        <selectKey keyProperty="dogId" resultType="long" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>

    <!-- 반려견 단건 조회 -->
    <select id="selectDogById" resultMap="dogResultMap">
        SELECT
            dog_id,
            name,
            breed,
            age,
            gender,
            is_neutered,
            weight,
            personality,
            habits,
            health_info,
            profile_image,
            created_at,
            updated_at
        FROM dog
        WHERE dog_id = #{dogId}
          AND is_deleted = 0
    </select>

    <!-- 반려견 조회 (소유자 확인) -->
    <select id="selectDogByIdAndUserId" resultMap="dogResultMap">
        SELECT
            dog_id,
            name,
            breed,
            age,
            gender,
            is_neutered,
            weight,
            personality,
            habits,
            health_info,
            profile_image,
            created_at,
            updated_at
        FROM dog
        WHERE dog_id = #{dogId}
          AND user_id = #{userId}
          AND is_deleted = 0
    </select>

    <!-- 반려견 리스트 조회 (본인) -->
    <select id="selectDogsByUserId" resultMap="dogResultMap">
        SELECT
            dog_id,
            name,
            breed,
            age,
            gender,
            is_neutered,
            weight,
            personality,
            habits,
            health_info,
            profile_image,
            created_at,
            updated_at
        FROM dog
        WHERE user_id = #{userId}
          AND is_deleted = 0
        ORDER BY created_at DESC
    </select>

    <!-- 반려견 리스트 조회 (타인) -->
    <select id="selectDogsByUsername" resultMap="dogResultMap">
        SELECT
            d.dog_id,
            d.name,
            d.breed,
            d.age,
            d.gender,
            d.is_neutered,
            d.weight,
            d.personality,
            d.habits,
            d.health_info,
            d.profile_image,
            d.created_at,
            d.updated_at
        FROM dog d
        INNER JOIN user u ON d.user_id = u.user_id
        WHERE u.user_name = #{username}
          AND d.is_deleted = 0
          AND u.is_deleted = 0
        ORDER BY d.created_at DESC
    </select>

    <!-- 반려견 정보 수정 -->
    <update id="updateDog">
        UPDATE dog
        <set>
            <if test="request.name != null and request.name != ''">
                name = #{request.name},
            </if>
            <if test="request.breed != null and request.breed != ''">
                breed = #{request.breed},
            </if>
            <if test="request.age != null">
                age = #{request.age},
            </if>
            <if test="request.gender != null and request.gender != ''">
                gender = #{request.gender},
            </if>
            <if test="request.isNeutered != null">
                is_neutered = #{request.isNeutered},
            </if>
            <if test="request.weight != null">
                weight = #{request.weight},
            </if>
            <if test="request.personality != null and request.personality != ''">
                personality = #{request.personality},
            </if>
            <if test="request.habits != null and request.habits != ''">
                habits = #{request.habits},
            </if>
            <if test="request.healthInfo != null and request.healthInfo != ''">
                health_info = #{request.healthInfo},
            </if>
            <if test="request.profileImageUrl != null and request.profileImageUrl != ''">
                profile_image = #{request.profileImageUrl},
            </if>
            updated_by = #{updatedBy},
            updated_at = NOW()
        </set>
        WHERE dog_id = #{dogId}
        AND user_id = #{userId}
        AND is_deleted = 0
    </update>

    <!-- 반려견 삭제 (소프트 삭제) -->
    <update id="deleteDog">
        UPDATE dog
        SET is_deleted = 1,
            deleted_at = NOW(),
            updated_by = #{deletedBy},
            updated_at = NOW()
        WHERE dog_id = #{dogId}
          AND user_id = #{userId}
          AND is_deleted = 0
    </update>

    <!-- 이름 중복 체크 -->
    <select id="existsByUserIdAndName" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM dog
            WHERE user_id = #{userId}
              AND name = #{name}
              AND is_deleted = 0
        )
    </select>

</mapper>